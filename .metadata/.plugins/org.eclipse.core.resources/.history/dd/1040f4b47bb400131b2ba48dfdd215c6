package com.jbentley.spotmapper;

import android.content.Context;
import android.content.SharedPreferences;
import android.content.SharedPreferences.Editor;
import android.preference.PreferenceManager;
import com.google.android.gms.location.Geofence;


//	//shared prefs for geofences
//	SharedPreferences mSharedGeoPrefs = PreferenceManager.getDefaultSharedPreferences(this);
//	
//	//editor for sharedGeoPrefs
//	Editor geoPrefEdit = mSharedGeoPrefs.edit();
//	
//	geoPrefEdit.putFloat(LAT_KEY, (float) geofence.getLat());
//	geoPrefEdit.putFloat(LNG_KEY, (float) geofence.getLng());
//	geoPrefEdit.putFloat(RADIUS_KEY, geofence.getRadius());
//	geoPrefEdit.putLong(EXPIRATION_KEY, geofence.getExpiration());
//	geoPrefEdit.putInt(TRANSITION_KEY, geofence.getTransition());
//	
//	geoPrefEdit.commit();

